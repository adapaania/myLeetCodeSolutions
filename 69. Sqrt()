class Solution:
    def mySqrt(self, x: int) -> int:
        ans=None
        for i in range (0,x+1,1):
            if i*i<=x:
                ans = i
            else: break
        
        return ans

sol = Solution()
x= 25
print(sol.mySqrt(x))

